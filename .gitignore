node_modules/
sessions/
runs/
*.sqlite
.env
# Per-install state, not source. Committing it meant every deployment
# inherited the last committer's client list without the runs/ that give it
# meaning — see config.example.json, which loadConfig() seeds from.
config.json
config.json.bak
config.json.tmp
# The dashboard/funnel login password (hashed, then encrypted at rest).
auth/

# Business strategy — NEVER commit. This repo is public.
# Pricing, margins, runway, GTM, competitor analysis, vendor negotiations and
# unshipped monetisation mechanics are all things a competitor reads for free
# and a prospect reads before a pricing conversation. Technical docs live in
# docs/ and are fine public; anything commercial lives here and stays local.
docs/private/
# Launch/sales assets: cold email, pitch decks, marketplace listings. These were
# public until 2026-09-09 — they remain in git history, so treat anything in here
# written before that date as already disclosed.
docs/launch/

# OS cruft
.DS_Store
Thumbs.db

# social/marketing drafts + captured media — never shipped in the repo
social/
CONTRAST_COMPLETION_CHECKLIST.md